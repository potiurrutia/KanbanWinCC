PORT=4000
JWT_SECRET=cambia-este-secreto-por-uno-largo-y-aleatorio
# Opcional: si defines DATABASE_URL se usa PostgreSQL; si no, SQLite.
# DATABASE_URL=postgresql://usuario:contrasena@host:5432/taskflow
# DATABASE_PATH=ruta/opcional/taskflow.db
